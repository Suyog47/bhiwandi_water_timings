import 'package:flutter/material.dart';
import 'colors.dart';

const dividerDark = Divider(height: 2, color: blackColor);

const dividerLight = Divider(height: 2, color: whiteColor);

const dividerGrey = Divider(height: 2, color: greyColor);